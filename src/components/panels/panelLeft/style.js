import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    width: '200px',
    position: 'fixed',
    left:0,
    top:0,
    bottom:0,
    backgroundColor: 'red'
});

export default styles;