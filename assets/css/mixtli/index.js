import {StyleSheet} from 'react-native';
import {constants,classes} from './../global';

export const mixtli = StyleSheet.create({
    link:{
        color: '#1566cc'
    }
});

export default mixtli;